From Coq Require Import Unicode.Utf8.
Require Category Import Category.Theory.
(* Variables A B C : Prop. *)
(* Check (A → B). *)
(* Goal (A → B → C) → (A → B) → A → C. *)
(* intros F G H. *)
(* apply F.  *)
(* - exact H. *)
(* - apply G, H. *)
(* Qed. *)


(* Goal (A ∧ B) → B ∧ A. *)
(* intro H. *)
(* elim H. *)
(* Qed. *)
